# 本地多模态 AI 助手

## 启动步骤
1. **Ollama:** 确保已运行并下载了 `qwen2.5` 和 `llava`。
2. **Backend:** 进入 `backend` 文件夹，执行 `mvn spring-boot:run`。
3. **Frontend:** 进入 `frontend` 文件夹，执行 `npm install` 然后 `npm start`。
4. **环境:** 确保系统已安装 FFmpeg 和 Whisper CLI 以支持音视频处理。
